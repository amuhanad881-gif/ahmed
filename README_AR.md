# 🚀 Squad Talk - Voice & Text Chat

تطبيق دردشة صوتية ونصية بتصميم احترافي يشبه Discord

## ⚡ تشغيل سريع

### الطريقة الأسهل (Docker):
```bash
# شغل السكريبت التلقائي
./start.sh

# أو مباشرة:
docker-compose up -d --build
```

### بدون Docker:
```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تشغيل التطبيق
python3 app.py
```

## 📋 قبل البدء

### تعديل إعدادات الإيميل
افتح `app.py` وعدل هذه الأسطر (حوالي السطر 25):

```python
EMAIL_CONFIG = {
    'smtp_server': 'smtp.gmail.com',
    'smtp_port': 587,
    'sender_email': 'بريدك@gmail.com',      # غير هذا
    'sender_password': 'كلمة-سر-التطبيق'     # غير هذا
}
```

**مهم**: استخدم "App Password" من Gmail، ليس كلمة سر حسابك!

### كيف تحصل على Gmail App Password:
1. اذهب لـ Google Account Settings
2. Security → 2-Step Verification (لازم يكون مفعل)
3. App passwords → أنشئ كلمة سر جديدة
4. استخدمها في الكود

## 🌐 الوصول للتطبيق

بعد التشغيل، افتح المتصفح على:
- محلي: `http://localhost:5000`
- شبكة: `http://IP-السيرفر:5000`

## 📁 الملفات المهمة

- `app.py` - التطبيق الرئيسي
- `requirements.txt` - المكتبات المطلوبة
- `wsgi.py` - ملف التشغيل للـ production
- `Dockerfile` - لبناء Docker image
- `docker-compose.yml` - لتشغيل التطبيق بسهولة
- `nginx.conf` - إعدادات Nginx للبروكسي
- `start.sh` - سكريبت تشغيل تلقائي
- `DEPLOYMENT_GUIDE_AR.md` - دليل نشر شامل بالعربي

## ✨ المميزات

- 🎤 دردشة صوتية حية
- 💬 دردشة نصية فورية
- 👥 نظام طلبات الصداقة
- 📧 تسجيل دخول بـ Gmail
- 😊 دعم الإيموجي
- ⭐ نظام Premium
- 🎨 تصميم احترافي
- 🔐 أمان متقدم

## 🛠️ للنشر على الإنترنت

اقرأ الدليل الشامل: `DEPLOYMENT_GUIDE_AR.md`

### خيارات سريعة:
1. **Railway.app** - مجاني وسهل
2. **Render.com** - مجاني للبداية
3. **DigitalOcean** - احترافي
4. **Heroku** - تقليدي ومعروف

## 🔧 أوامر مفيدة

### Docker:
```bash
docker-compose up -d          # تشغيل
docker-compose down           # إيقاف
docker-compose logs -f        # مشاهدة اللوقز
docker-compose restart        # إعادة تشغيل
```

### بدون Docker:
```bash
python3 app.py                # تشغيل
pkill -f app.py               # إيقاف
tail -f squadtalk.log         # مشاهدة اللوقز
```

## 🔑 Owner Code

الكود السري للحصول على Premium مجاناً:
```
i'm the owner
```
استخدمه في نافذة الترقية للحصول على Premium!

## 📞 الدعم

إذا واجهتك مشاكل:
1. تأكد من تعديل إعدادات الإيميل
2. تأكد من تثبيت المكتبات
3. شوف اللوقز للأخطاء
4. اقرأ دليل النشر الشامل

## 🎉 استمتع!

جرب التطبيق واستمتع بالدردشة مع أصدقائك!
